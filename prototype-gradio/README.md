# CodeAtlas — Gradio prototype (optional)

Placeholder for the optional, prototype-tier Gradio frontend (CLAUDE.md §7b).

It will be a **thin** pure-Python app that calls the same FastAPI backend over
HTTP — it does **not** re-implement ingestion or retrieval. Not implemented in
the bootstrap task; added later.
